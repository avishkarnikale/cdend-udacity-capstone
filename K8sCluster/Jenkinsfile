pipeline {
	agent any
	stages {

		stage('Create kubernetes cluster') {
			steps {
				withAWS(region:'ap-southeast-1', credentials:'aws-static') {
					sh 'echo "Hello World"'
      			        }
			}
  }
}
     
